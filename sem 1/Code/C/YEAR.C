#include<stdio.h>
#include<conio.h>
void main(){
	int year;
	clrscr();
	printf("Enter a year:");
	scanf("%d",&year);
	if (year%=4)
	{printf("The year is a leap year.");}
	else {printf("The year is not a leap year.");}
	getch();
	}
